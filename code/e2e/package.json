{
  "name": "call-calendar-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "End-to-end сценарии проекта «Календарь звонков»",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.3",
    "typescript": "~5.9.0"
  }
}
